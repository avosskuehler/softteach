<UserControl x:Class="SoftTeach.View.Wochenpläne.GanztagsterminSingleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="200" >
  <Border Background="{Binding Path=TerminplaneintragBackground}" CornerRadius="1">
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" MaxWidth="80"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <Label Grid.Column="0" Padding="5,0,5,0" VerticalAlignment="Center" Content="{Binding Path=TerminplaneintragKlasse}"/>
      <TextBlock Grid.Column="1" Padding="5,0,5,0" VerticalAlignment="Top" Text="{Binding Path=TerminplaneintragThema}" TextWrapping="Wrap"/>
    </Grid>
  </Border>
</UserControl>
