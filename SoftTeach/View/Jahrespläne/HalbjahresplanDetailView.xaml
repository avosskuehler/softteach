<UserControl x:Class="SoftTeach.View.Jahrespläne.HalbjahresplanDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Jahrespläne="clr-namespace:SoftTeach.View.Jahrespläne"
             xmlns:Jahrespläne1="clr-namespace:SoftTeach.ViewModel.Jahrespläne" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <UserControl.Resources>
    <DataTemplate DataType="{x:Type Jahrespläne1:MonatsplanViewModel}">
      <Jahrespläne:MonatsplanDetailView />
    </DataTemplate>
  </UserControl.Resources>
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition/>
      <ColumnDefinition/>
      <ColumnDefinition/>
      <ColumnDefinition/>
      <ColumnDefinition/>
    </Grid.ColumnDefinitions>
    <ContentControl Grid.Column="0" Content="{Binding Path=Monat1}"/>
    <ContentControl Grid.Column="1" Content="{Binding Path=Monat2}"/>
    <ContentControl Grid.Column="2" Content="{Binding Path=Monat3}"/>
    <ContentControl Grid.Column="3" Content="{Binding Path=Monat4}"/>
    <ContentControl Grid.Column="4" Content="{Binding Path=Monat5}"/>
    <ContentControl Grid.Column="5" Content="{Binding Path=Monat6}"/>
  </Grid>
</UserControl>
