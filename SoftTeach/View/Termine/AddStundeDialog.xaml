<Window x:Class="SoftTeach.View.Termine.AddStundeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:resources="clr-namespace:SoftTeach.Resources" 
        xmlns:Jahrespläne="clr-namespace:SoftTeach.View.Jahrespläne"
        xmlns:StundenentwürfeView="clr-namespace:SoftTeach.View.Stundenentwürfe"
        xmlns:StundenentwürfeViewModel="clr-namespace:SoftTeach.ViewModel.Stundenentwürfe"
        xmlns:Termine="clr-namespace:SoftTeach.ViewModel.Termine" xmlns:controls="clr-namespace:SoftTeach.Resources.Controls"
        xmlns:termine="clr-namespace:SoftTeach.View.Termine"
        Title="Stunde ..." 
        Icon="/SoftTeach;component/Images/Stundenentwurf32.png"
        Height="600" 
        Width="900">
  <Window.Resources>
    <DataTemplate DataType="{x:Type StundenentwürfeViewModel:StundenentwurfWorkspaceViewModel}">
      <StundenentwürfeView:StundenentwurfWorkspaceView />
    </DataTemplate>
  </Window.Resources>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <controls:SmallDialogHeader Grid.Row="0" Title="Neue Stunde anlegen ..."
                                      Icon="/SoftTeach;component/Images/Stundenentwurf32.png"/>

    <Grid Background="{StaticResource DefaultOfficeBackgroundBrush}" Grid.Row="1" >
      <ContentControl Content="{Binding}">
        <ContentControl.Resources>
          <DataTemplate DataType="{x:Type Termine:StundeViewModel}">
            <termine:StundeDetailView />
          </DataTemplate>
        </ContentControl.Resources>
      </ContentControl>
    </Grid>
    <Grid Background="{StaticResource DefaultButtonsBackgroundBrush}" Grid.Row="2">
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
        <Button Width="100" IsDefault="True" Click="OkClick" TabIndex="2">OK</Button>
        <Label></Label>
        <Button Width="100" IsCancel="True" Click="CancelClick">Abbrechen</Button>
      </StackPanel>
    </Grid>
  </Grid>
</Window>
