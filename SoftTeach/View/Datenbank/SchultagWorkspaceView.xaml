<Window x:Class="SoftTeach.View.Datenbank.SchultagWorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
         xmlns:fa="clr-namespace:SoftTeach.Resources.FontAwesome"
            xmlns:resources="clr-namespace:SoftTeach.Resources" xmlns:SoftTeach="clr-namespace:SoftTeach"
        xmlns:Globalization="clr-namespace:System.Globalization;assembly=mscorlib"
        xmlns:controls="clr-namespace:SoftTeach.Resources.Controls" mc:Ignorable="d" 
        Title="Datenbank - Schultage"
        Icon="/SoftTeach;component/Images/Database.ico"
             d:DesignHeight="400" d:DesignWidth="800" WindowStyle="SingleBorderWindow">
  <Window.Resources>
  </Window.Resources>
  <Grid Background="{StaticResource MainBackgroundBrush}">
    <Grid.RowDefinitions>
      <RowDefinition Height="48"/>
      <RowDefinition/>
      <RowDefinition Height="32"/>
    </Grid.RowDefinitions>
    <controls:SmallDialogHeader  Icon="/SoftTeach;component/Images/Schultag32.png" 
                              Grid.Row="0" 
                              Title="Das ist der Inhalt der Schultage-Tabelle. Diese Darstellung dient nur zur Kontrolle. 
Schultage ändern Sie im Schultag Modul."/>
    <GroupBox Grid.Row="1" Header="Tabelle der Schultage" Padding="3" Margin="5">
      <DataGrid ItemsSource="{Binding Source={x:Static SoftTeach:App.MainViewModel}, Path=Schultage}" AutoGenerateColumns="False"
                IsReadOnly="True">
        <DataGrid.Columns>
          <DataGridTextColumn Header="Schuljahr" Binding="{Binding Path=Model.Schulwoche.Schuljahr.Bezeichnung}"/>
          <DataGridTextColumn Header="Monat" Binding="{Binding Path=SchultagDatum.Month}"/>
          <DataGridTextColumn Header="Datum" Binding="{Binding Path=SchultagDatum.Date, StringFormat={}{0:dd. MMMM yyyy},ConverterCulture={x:Static Globalization:CultureInfo.CurrentCulture}}"/>
          <DataGridTextColumn Header="Beschreibung" Binding="{Binding Path=SchultagTermintyp.TermintypBezeichnung}"  Width="*"/>
        </DataGrid.Columns>
      </DataGrid>
    </GroupBox>
    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Name="OK" Width="80" Margin="3" IsDefault="True" Click="OKClick">OK</Button>
    </StackPanel>
  </Grid>
</Window>
