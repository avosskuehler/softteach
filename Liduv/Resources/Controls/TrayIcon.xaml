<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:tb="http://www.hardcodet.net/taskbar">
  <!-- Globally declared notify icon -->
  <tb:TaskbarIcon x:Key="NotenNotifyIcon"
                  IconSource="/Liduv;component/Images/Inactive.ico"
                  ToolTipText=""
                  Visibility="Hidden"
                  PopupActivation="LeftClick">
    <tb:TaskbarIcon.TrayToolTip>
      <Border
      Background="White"
      BorderBrush="DarkGray"
      BorderThickness="1"
      CornerRadius="0"
      Opacity="1"
      Width="Auto"
      Height="25">
        <TextBlock Text="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Border}}, Path=(tb:TaskbarIcon.ParentTaskbarIcon).ToolTipText}"
        HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5"/>
      </Border>
    </tb:TaskbarIcon.TrayToolTip>
  </tb:TaskbarIcon>
</ResourceDictionary>